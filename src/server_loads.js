const loadBody = ()  =>{
  return loadPartialHTML("page-body-index");
}

const LoadHearders = () => {
  return loadPartialHTML("page-header-index");
}

const loadLogin = () => {
  return loadPartialHTML("page-login");
}

const loadFooter = () => {
  return loadPartialHTML("page-footer-index");
}

const loadHeaderAdmin = () => {
  return loadPartialHTML("page-header-admin");
}
const loadManutencao = () => {
  return loadPartialHTML("page-construction");
}
const loadListCadastro = () => {
  return loadPartialHTML("page-cadastro-lista");
}
const loadCadastro = () => {
  return loadPartialHTML("page-cadastro-n");
}
const loadUser = () => {
  return loadPartialHTML("page-user");
}
const loadPedido = () => {
  return loadPartialHTML("page-pedido-lista");
}
const loadnewPedido = () => {
  return loadPartialHTML("page-pedido");
}